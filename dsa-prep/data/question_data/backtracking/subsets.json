{
    "id": 9,
    "leetcode_number": 78,
    "title": "Subsets",
    "difficulty": "Medium",
    "url": "https://leetcode.com/problems/subsets/",
    "problem_statement": "Given an integer array nums of unique elements, return all possible subsets (the power set).",
    "examples": [
        {
            "input": "nums = [1,2,3]",
            "output": "[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]",
            "explanation": "All subsets of the distinct set."
        },
        {
            "input": "nums = [0]",
            "output": "[[],[0]]",
            "explanation": "Single element yields the empty and singleton subsets."
        }
    ],
    "constraints": [
        "1 ≤ nums.length ≤ 10",
        "-10 ≤ nums[i] ≤ 10",
        "All nums are unique."
    ],
    "intuition": "Backtrack by choosing whether to include each element, generating all 2^n subsets.",
    "approaches": [
        {
            "name": "Choose or Skip",
            "badge": "Optimal",
            "badge_color": "green",
            "description": "For each index, recursively explore both including and excluding the current element.",
            "time": "O(2^n)",
            "space": "O(n)",
            "why_not": null,
            "code": "class Solution {\n    public List<List<Integer>> subsets(int[] nums) {\n        List<List<Integer>> result = new ArrayList<>();\n        backtrack(nums, 0, new ArrayList<>(), result);\n        return result;\n    }\n\n    private void backtrack(int[] nums, int index, List<Integer> current, List<List<Integer>> result) {\n        if (index == nums.length) {\n            result.add(new ArrayList<>(current));\n            return;\n        }\n        // Exclude current number\n        backtrack(nums, index + 1, current, result);\n        // Include current number\n        current.add(nums[index]);\n        backtrack(nums, index + 1, current, result);\n        current.remove(current.size() - 1);\n    }\n}"
        }
    ],
    "interview_qa": [
        {
            "q": "How many subsets does an n-element set have?",
            "a": "There are 2^n subsets, including the empty set and the set itself."
        },
        {
            "q": "Why make copies of current?",
            "a": "Each leaf of recursion must keep its own subset independent of later changes during backtracking."
        }
    ]
}