{
    "id": 5,
    "leetcode_number": 4,
    "title": "Median of Two Sorted Arrays",
    "difficulty": "Hard",
    "url": "https://leetcode.com/problems/median-of-two-sorted-arrays/",
    "problem_statement": "Given two sorted arrays, return the median of the combined array in O(log(m+n)) time.",
    "examples": [
        {
            "input": "nums1 = [1,3], nums2 = [2]",
            "output": "2.0",
            "explanation": "The merged array is [1,2,3]."
        },
        {
            "input": "nums1 = [1,2], nums2 = [3,4]",
            "output": "2.5",
            "explanation": "The merged array is [1,2,3,4]."
        }
    ],
    "constraints": [
        "nums1.length == m",
        "nums2.length == n",
        "0 ≤ m, n ≤ 1000",
        "-10⁶ ≤ nums[i] ≤ 10⁶",
        "The overall run time complexity should be O(log(m+n))."
    ],
    "intuition": "Binary search on the smaller array to partition both arrays such that left halves and right halves satisfy median properties.",
    "approaches": [
        {
            "name": "Partition Binary Search",
            "badge": "Optimal",
            "badge_color": "green",
            "description": "Partition both arrays around the median and use the max left and min right values to compute the median.",
            "time": "O(log(min(m,n)))",
            "space": "O(1)",
            "why_not": null,
            "code": "class Solution {\n    public double findMedianSortedArrays(int[] nums1, int[] nums2) {\n        if (nums1.length > nums2.length) return findMedianSortedArrays(nums2, nums1);\n        int m = nums1.length, n = nums2.length;\n        int low = 0, high = m;\n        while (low <= high) {\n            int i = (low + high) / 2;\n            int j = (m + n + 1) / 2 - i;\n            int maxLeftA = (i == 0) ? Integer.MIN_VALUE : nums1[i - 1];\n            int minRightA = (i == m) ? Integer.MAX_VALUE : nums1[i];\n            int maxLeftB = (j == 0) ? Integer.MIN_VALUE : nums2[j - 1];\n            int minRightB = (j == n) ? Integer.MAX_VALUE : nums2[j];\n            if (maxLeftA <= minRightB && maxLeftB <= minRightA) {\n                if ((m + n) % 2 == 1) {\n                    return Math.max(maxLeftA, maxLeftB);\n                }\n                return (Math.max(maxLeftA, maxLeftB) + Math.min(minRightA, minRightB)) / 2.0;\n            } else if (maxLeftA > minRightB) {\n                high = i - 1;\n            } else {\n                low = i + 1;\n            }\n        }\n        throw new IllegalArgumentException();\n    }\n}"
        }
    ],
    "interview_qa": [
        {
            "q": "Why binary search the smaller array?",
            "a": "Binary searching the smaller array keeps the partition space minimal and avoids index issues."
        },
        {
            "q": "How is the median computed from partition edges?",
            "a": "Use the larger of left-side maxima and the smaller of right-side minima, with one or two elements depending on parity."
        }
    ]
}