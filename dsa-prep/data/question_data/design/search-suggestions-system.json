{
    "id": 8,
    "leetcode_number": null,
    "title": "Search Suggestions System",
    "difficulty": "Hard",
    "url": "https://leetcode.com/problems/search-suggestions-system/",
    "problem_statement": "Build a search suggestion system that returns up to three lexicographically sorted product names matching a search prefix.",
    "examples": [
        {
            "input": "Example input",
            "output": "Example output",
            "explanation": "Example explanation will be added here."
        }
    ],
    "constraints": [
        "Operations up to 10⁵",
        "Keys/values within standard ranges"
    ],
    "intuition": "Choose data structures that optimize for the required operations.",
    "approaches": [
        {
            "name": "Optimal Solution",
            "badge": "Optimal",
            "badge_color": "green",
            "description": "A clean explanation of the optimal approach will be added here.",
            "time": "",
            "space": "",
            "why_not": null,
            "code": "class Solution {\n    public List<List<String>> suggestedProducts(String[] products, String searchWord) {\n        Arrays.sort(products);\n        List<List<String>> res = new ArrayList<>();\n        String prefix = \"\";\n        for (char c : searchWord.toCharArray()) {\n            prefix += c;\n            List<String> list = new ArrayList<>();\n            int idx = Arrays.binarySearch(products, prefix);\n            if (idx < 0) idx = -idx - 1;\n            for (int i = idx; i < products.length && list.size() < 3; i++) {\n                if (products[i].startsWith(prefix)) list.add(products[i]);\n            }\n            res.add(list);\n        }\n        return res;\n    }\n}"
        }
    ],
    "interview_qa": [
        {
            "q": "What is the key idea?",
            "a": "Choose the topic-specific pattern that best fits the problem and avoid naive brute force."
        },
        {
            "q": "How do you handle edge cases?",
            "a": "Check for empty or minimal input before applying the main algorithm."
        }
    ]
}