{
    "id": 12,
    "leetcode_number": 684,
    "title": "Redundant Connection",
    "difficulty": "Medium",
    "url": "https://leetcode.com/problems/redundant-connection/",
    "problem_statement": "In this problem, a tree is an undirected graph that is connected and has no cycles. You are given a graph that started as a tree with n nodes labeled from 1 to n, with one additional edge added. The added edge has two different vertices chosen from 1 to n, and was not an edge that already existed. The graph is represented as an array of edges where edges[i] = [ai, bi] indicates that there is an undirected edge between nodes ai and bi in the graph. Return an edge that can be removed so that the resulting graph is a tree of n nodes. If there are multiple answers, return the answer that occurs last in the input.",
    "examples": [
        {
            "input": "edges = [[1,2],[1,3],[2,3]]",
            "output": "[2,3]",
            "explanation": "The given graph has nodes 1, 2, and 3. The edge [2,3] is redundant because it creates a cycle."
        },
        {
            "input": "edges = [[1,2],[2,3],[3,4],[1,4],[1,5]]",
            "output": "[1,4]",
            "explanation": "The given graph has nodes 1, 2, 3, 4, and 5. The edge [1,4] is redundant because it creates a cycle."
        }
    ],
    "constraints": [
        "n == edges.length",
        "3 <= n <= 1000",
        "edges[i].length == 2",
        "1 <= ai < bi <= edges.length",
        "ai != bi",
        "There are no repeated edges.",
        "The given graph is connected and has exactly one cycle."
    ],
    "intuition": "The graph is almost a tree with one extra edge causing a cycle. Use Union-Find to find the edge that connects already connected nodes.",
    "approaches": [
        {
            "name": "Union-Find",
            "badge": "Optimal",
            "badge_color": "green",
            "description": "Union nodes for each edge. The first edge where nodes are already in same component is redundant.",
            "time": "O(E * α(N))",
            "space": "O(N)",
            "why_not": "DFS can find cycles, but Union-Find directly identifies the edge.",
            "code": "class Solution {\n    public int[] findRedundantConnection(int[][] edges) {\n        int n = edges.length;\n        int[] parent = new int[n + 1];\n        for (int i = 1; i <= n; i++) parent[i] = i;\n        for (int[] edge : edges) {\n            int u = find(parent, edge[0]);\n            int v = find(parent, edge[1]);\n            if (u == v) return edge;\n            parent[u] = v;\n        }\n        return new int[0];\n    }\n    private int find(int[] parent, int x) {\n        return parent[x] == x ? x : (parent[x] = find(parent, parent[x]));\n    }\n}",
            "interview_commentary": [
                "Process edges in order; union if not connected.",
                "If already connected, that's the redundant edge.",
                "Since only one cycle, this works."
            ]
        }
    ],
    "interview_qa": [
        {
            "q": "Why return the last in input if multiple?",
            "a": "Problem says return the one that occurs last, but since only one cycle, it's the first found."
        },
        {
            "q": "What if no cycle?",
            "a": "Problem guarantees one cycle."
        },
        {
            "q": "Can use DFS?",
            "a": "Yes, but need to track edges."
        },
        {
            "q": "Time complexity?",
            "a": "Almost O(E) due to α(N)."
        }
    ]
}
