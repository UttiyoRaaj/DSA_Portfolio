{
    "id": 2,
    "leetcode_number": 3,
    "title": "Longest Substring Without Repeating Characters",
    "difficulty": "Medium",
    "url": "https://leetcode.com/problems/longest-substring-without-repeating-characters/",
    "problem_statement": "Find the length of the longest substring without repeating characters.",
    "examples": [
        {
            "input": "s = \"abcabcbb\"",
            "output": "3",
            "explanation": "The answer is \"abc\", with length 3."
        }
    ],
    "constraints": [
        "0 <= s.length <= 5 * 10^4",
        "s consists of English letters, digits, symbols and spaces."
    ],
    "intuition": "Use a sliding window and a map or array to track the last seen positions of characters.",
    "approaches": [
        {
            "name": "Sliding window with last-seen index",
            "badge": "Optimal",
            "badge_color": "green",
            "description": "Expand the right edge and move the left edge past repeated characters using last seen positions.",
            "time": "O(n)",
            "space": "O(1)",
            "why_not": "Brute force checking each substring is O(n^2).",
            "code": "class Solution {\n    public int lengthOfLongestSubstring(String s) {\n        int[] index = new int[128];\n        int maxLen = 0;\n        int left = 0;\n        for (int right = 0; right < s.length(); right++) {\n            left = Math.max(left, index[s.charAt(right)]);\n            maxLen = Math.max(maxLen, right - left + 1);\n            index[s.charAt(right)] = right + 1;\n        }\n        return maxLen;\n    }\n}"
        }
    ],
    "interview_qa": [
        {
            "q": "How do you update the left boundary?",
            "a": "Move it to one past the previous index of a repeating character."
        },
        {
            "q": "Why store index+1 instead of index?",
            "a": "It allows using zero as the default unseen value."
        }
    ]
}
